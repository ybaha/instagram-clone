import React, { useContext, useState, useEffect } from "react";
import { auth, db } from "./firebase";
import firebase from "firebase/app";

type Props = {
  useAuth: any;
};

const AuthProvider: React.FC<Props> = ({ useAuth }) => {
  return <></>;
};
